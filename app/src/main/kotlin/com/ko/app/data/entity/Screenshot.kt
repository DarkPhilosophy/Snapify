package com.ko.app.data.entity

// Entity is provided by :core module (com.ko.app.data.entity.Screenshot)
// Keep this stub so references in the app module compile while the canonical entity lives in :core.
