package ro.snapify.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Legacy semantic colors kept for older call sites.
 * New code must consume SnapifyTheme.colors tokens instead.
 */
val SuccessGreen = Color(0xFFA3C98B)
val WarningOrange = Color(0xFFE0B45C)
val ErrorRed = Color(0xFFE07A68)
